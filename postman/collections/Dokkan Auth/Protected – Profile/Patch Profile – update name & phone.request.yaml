$kind: http-request
url: "{{baseUrl}}/auth/profile"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "name": "Updated Name",
      "contactNumber": "01234567890"
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 3000
