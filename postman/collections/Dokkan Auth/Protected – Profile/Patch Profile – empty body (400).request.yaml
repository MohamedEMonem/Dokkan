$kind: http-request
url: "{{baseUrl}}/auth/profile"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: json
  content: "{}"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 8000
